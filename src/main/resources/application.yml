server:
  port: 9717

spring:
  application:
    name: route-warning-service

  datasource:
    driver-class-name: com.xugu.cloudjdbc.Driver
    url: jdbc:xugu://10.159.90.202:36034/ythpt?characterEncoding=UTF-8
    username: SYSDBA
    password: Sysdba2024@Xugu
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      test-while-idle: true
      validation-query: SELECT 1

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.warning.entity
  type-handlers-package: com.warning.typehandler
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

# 网格数据接口配置
grid:
  api:
    base-url: http://172.22.96.169/gridserv/api/getSevpEleByTime
    time-list-url: http://172.22.96.169/gridserv/api/getTimeListByEle
    api-key: 9c390758184ad0d58dd95bdfdfd6552a

# 日志配置
logging:
  level:
    com.warning: info
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n'
